## Meta functions
citation("linbin")
packageDescription("linbin")
packageVersion("linbin")

## Functions
# create
example(events)
example(as_events)
#example(read_events)
example(seq_events)
# tests
example(is_events)
example(sort_events)
example(has_overlapping_events)
# metrics
example(event_range)
example(event_coverage)
example(event_overlaps)
example(event_gaps)
# operations
example(fill_event_gaps)
example(transform_events)
example(crop_events)
example(cut_events)
# sampling
example(find_intersecting_events)
example(sample_events)
# plotting
example(plot_events)